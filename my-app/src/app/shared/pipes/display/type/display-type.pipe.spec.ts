import { DisplayTypePipe } from './display-type.pipe';

describe('DisplayTypePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayTypePipe();
    expect(pipe).toBeTruthy();
  });
});
