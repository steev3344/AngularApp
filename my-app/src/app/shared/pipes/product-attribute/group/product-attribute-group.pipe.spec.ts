import { ProductAttributeGroupPipe } from './product-attribute-group.pipe';

describe('ProductAttributeGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductAttributeGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
